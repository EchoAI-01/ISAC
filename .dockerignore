# .dockerignore - 排除不必要的文件加速构建

# Git 与 IDE
.git
.gitignore
.github
.idea
.vscode

# Python 缓存与虚拟环境
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 临时与构建产物
.tmpfiles/
dist/
build/
*.egg

# 测试与文档 (运行时不需要)
tests/
docs/
*.md
!README.md  # 保留 README (Dockerfile 引用)

# 数据 (避免镜像内置用户数据, 通过 volume 挂载); 但保留随包默认资产 ——
# 2026-08-19: 此前 data/* 仅留 .gitkeep, COPY data/ 实际为空, 镜像内无
# model_capabilities.json (U7 能力路由静默降级)、无 pricing.jsonc (成本估算恒
# None)、无 catalogs。现放行这几项只读默认资产, 用户运行数据仍走 volume。
data/*
!data/.gitkeep
!data/config.sample.jsonc
!data/model_capabilities.json
!data/model_capabilities.overrides.json
!data/pricing.jsonc
!data/catalogs

# 系统文件
.DS_Store
*.log
