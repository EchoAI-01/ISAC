# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# 工具缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 运行数据 (保留目录占位; sample 配置模板例外, 不含敏感信息)
data/*
!data/.gitkeep
!data/config.sample.jsonc
# U7 模型能力快照 (models.dev 生成, CI 每周刷新, 见 .github/workflows/model-capabilities.yml)
!data/model_capabilities.json
!data/model_capabilities.overrides.json
# 默认模型价目表 (成本计量开箱可用, 2026-08-19 入仓)
!data/pricing.jsonc
# U8 治理门禁 catalog (生成物入库, CI --check 漂移检测)
!data/catalogs

# IDE
.idea/
.vscode/

# Claude Code 工作区 (worktrees / 临时会话文件)
.claude/
.tmpfiles/

# macOS
.DS_Store

# 日志
*.log

# 测试临时快照目录 (MVP-Fix: ConversationStateStore 隔离)
tests/integration/_tmp_agents/
